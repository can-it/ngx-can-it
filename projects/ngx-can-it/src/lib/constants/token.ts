import { InjectionToken } from '@angular/core';
import { Comparator } from '@can-it/core';

export const ACTION_OPERATOR = new InjectionToken<Comparator>('ACTION_OPERATOR');

export const RI_OPERATOR = new InjectionToken<Comparator>('RI_OPERATOR');
